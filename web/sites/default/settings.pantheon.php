<?php

/**
 * Pantheon configuration.
 *
 * Pantheon automatically provides the database connection.
 * We only define project-specific settings here.
 */

$settings['config_sync_directory'] = dirname(DRUPAL_ROOT) . '/config';
